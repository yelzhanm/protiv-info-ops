# 🛡️ Система противодействия информационным операциям

Веб-приложение на базе AI для анализа информационных потоков, выявления фейков, манипуляций и определения тональности текстов. Система объединяет интерфейс (Flask) и аналитическое ядро (FastAPI + NLP) в одном приложении.

---

## 📋 Предварительные требования

Перед началом работы убедитесь, что у вас установлены:

1.  **Python 3.10+**: [Скачать Python](https://www.python.org/downloads/)
2.  **Ollama**: Для работы локальной LLM (генерация саммари и анализ угроз). [Скачать Ollama](https://ollama.com/)
3.  **(Опционально) Neo4j**: Если планируется использование графовой базы данных для тезауруса.

---

## 🚀 Установка и настройка

### Шаг 1. Клонирование или загрузка проекта
Распакуйте архив с проектом или склонируйте репозиторий. Откройте терминал в папке проекта.

### Шаг 2. Создание виртуального окружения
Рекомендуется использовать виртуальное окружение, чтобы изолировать зависимости проекта.

**Windows:**
```bash
python -m venv .venv
.venv\Scripts\activate
macOS / Linux:

Bash

python3 -m venv .venv
source .venv/bin/activate
Шаг 3. Установка зависимостей
Установите необходимые библиотеки из файла requirements.txt:

Bash

pip install -r requirements.txt
Шаг 4. Загрузка моделей NLP
Для работы анализатора необходимо скачать языковую модель Spacy и модель для Ollama.

Скачивание модели Spacy (для русского языка):

Bash

python -m spacy download ru_core_news_sm
Настройка Ollama (LLM): Запустите приложение Ollama. В отдельном терминале выполните команду для скачивания нейросети.

Рекомендация: Если у вас мало оперативной памяти (<8-16 ГБ), используйте легкую модель tinyllama. Если ресурсов достаточно — llama3.

Bash

ollama pull tinyllama
# или
ollama pull llama3
Важно: Проверьте файл nlp.py. Переменная OLLAMA_MODEL должна соответствовать скачанной модели (по умолчанию может стоять 'llama3'). Если скачали tinyllama, измените строку на OLLAMA_MODEL = 'tinyllama'.

Шаг 5. Настройка переменных окружения (.env)
Создайте файл .env в корневой папке проекта и добавьте туда следующие настройки:

Фрагмент кода

# Секретный ключ для сессий Flask
SECRET_KEY=super-secret-key-change-it

# Настройки Neo4j (можно оставить пустыми, если база не используется)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=ваш_пароль

# Пароли для входа в систему (роли)
ADMIN_PASSWORD=admin123
ANALYST_PASSWORD=analyst123
LINGUIST_PASSWORD=linguist123
🗄️ Подготовка Базы Данных (SQLite)
Проект использует SQLite для хранения сообщений и результатов анализа. Перед первым запуском необходимо перенести начальные данные из файла project.json в базу данных, иначе аналитика будет пустой.

Выполните команду:

Bash

python migrate_to_sqlite.py
Ожидаемый результат: Вывод сообщения ✅ Дайын! Қосылған жазбалар: ... и создание файла data/db.sqlite.

▶️ Запуск приложения
Для запуска единого сервера (который обслуживает и интерфейс, и API) используйте команду:

Bash

python main.py
Если запуск прошел успешно, в терминале появится сообщение:

INFO:     Uvicorn running on [http://127.0.0.1:5000](http://127.0.0.1:5000)
Откройте браузер и перейдите по адресу: 👉 http://127.0.0.1:5000

🔑 Доступ в систему
На странице входа выберите роль. Пароли по умолчанию (если вы не меняли их в файле .env):

Администратор (доступ к админ-панели, анализ текстов, удаление записей):

Пароль: admin123

Аналитик (просмотр дашбордов, графиков и статистики):

Пароль: analyst123

Лингвист (управление тезаурусом терминов):

Пароль: linguist123

🛠️ Возможные проблемы и решения
Ошибка Ollama: model requires more system memory

Причина: Вашему компьютеру не хватает оперативной памяти (RAM) для запуска модели llama3.

Решение: В файле nlp.py замените OLLAMA_MODEL = 'llama3' на OLLAMA_MODEL = 'tinyllama'. Затем в терминале выполните ollama pull tinyllama.

Графики в разделе "Аналитика" пустые (по нулям)

Причина: База данных db.sqlite пуста или не создана.

Решение: Остановите сервер (Ctrl+C) и запустите скрипт миграции: python migrate_to_sqlite.py. Затем снова запустите python main.py.

Ошибка ModuleNotFoundError

Причина: Вы забыли активировать виртуальное окружение или не установили зависимости.

Решение: Повторите Шаг 2 (активация venv) и Шаг 3 (установка requirements.txt).